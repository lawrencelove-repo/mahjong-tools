# style-2

Lemons tile pack from **Maison Lude**.

Filenames use suit-letter first (`B1`–`B9`, `C1`–`C9`, `P1`–`P9`, winds `WE`/`WS`/`WW`/`WN`, dragons `DG`/`DR`/`DW`, flowers `F1`–`F4`, jokers `J1`/`J2`, blank `Blank`).

Missing tiles fall back to **style-1** at runtime.
